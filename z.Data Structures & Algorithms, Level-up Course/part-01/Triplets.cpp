#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

vector<vector<int>> Triplets(vector<int> arr, int target)
{
    int k = arr.size();
    for(let)

    return triplets;
}

int main()
{
    vector<int> numbers = {5, 2, 8, 1, 4, 2, 5, 8};
    Triplets(numbers,30);
    return 0;
}